# 2image v2.3.0

本次更新聚焦于数据目录迁移：将本地数据存放路径从 `~/.text_to_image_app` 迁移至 `~/2image`，提升目录可见性与易用性；同时内置一键自动迁移机制，确保旧版本用户升级后数据无缝衔接，旧目录完整保留作为备份。

## 主要更新

### 数据目录迁移

- **新目录路径**：所有本地数据（配置、历史数据库、生成图片、日志、字体缓存、自定义短语）统一存放于 `~/2image/`，不再是隐藏目录，用户更容易定位和管理。
- **自动迁移**：首次启动 v2.3.0 时，程序检测到新目录无配置且旧目录存在，自动将旧目录全部内容复制到新目录，旧目录保留作为备份，不会删除。
- **幂等安全**：已迁移过不会重复执行（通过检测新目录 `config.json` 判断）；新目录已有数据时不会被旧数据覆盖。
- **错误隔离**：单个文件复制失败不会中断整个迁移流程，失败项会在控制台输出警告。

### 路径引用更新

- `config/settings.py`：`APP_DIR` 常量从 `~/.text_to_image_app` 改为 `~/2image`，新增 `LEGACY_APP_DIR` 指向旧路径用于迁移。
- `main.py`：启动流程中在数据库初始化之前调用 `migrate_legacy_data()`，确保迁移先于任何数据操作。
- `config/fonts.py`、`ui/wizard_llm.py`：文档字符串与用户可见提示同步更新。
- `README.md` / `README.en.md`：数据目录表格与说明更新，添加升级说明。

## 下载与安装

- **安装包**：下载 `text2image_pro_v2.3.0.exe`，运行后按向导安装。
- **便携版**：下载 `text2image_pro.exe`，无需安装即可直接运行。
- **升级说明**：从 v2.2.0 及更早版本升级时，首次启动会自动将 `~/.text_to_image_app` 中的数据复制到 `~/2image`，旧目录保留作为备份。如需回退到旧版本，手动将数据移回原目录即可。

## 验证

- GitHub Actions：Ubuntu 质量门禁 + Windows 测试 + Windows 构建三阶段均已通过。
- 完整测试套件：307 项通过，覆盖率 54%+（阈值 50%）。
- 迁移逻辑专项测试：覆盖迁移触发、覆盖保护、全新安装三种场景。

**完整变更**：https://github.com/Aswellle/2image/compare/v2.2.0...v2.3.0
